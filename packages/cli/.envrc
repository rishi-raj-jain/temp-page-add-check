# direnv configuration for @creem_io/cli development
# Run `direnv allow` after cloning to enable

# Add bin/ to PATH (contains dev wrapper script)
PATH_add bin

# Mark as dev environment
export CREEM_DEV=1

# Helpful reminder
log_status "@creem_io/cli dev environment loaded. Run 'pnpm build' to compile changes."
