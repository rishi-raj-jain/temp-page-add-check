/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { moderationScreenPrompt } from "../../funcs/moderationScreenPrompt.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.ScreenPromptRequest$inboundSchema,
};

export const tool$moderationScreenPrompt: ToolDefinition<typeof args> = {
  name: "moderation-screen-prompt",
  description: `Screen a prompt

Evaluate a text prompt against content policies before generation. This endpoint is experimental and may change.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await moderationScreenPrompt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
